# =======================================
# Safe Profile Cordis Plugins Composition
# =======================================
- name: '@pi-cordis/plugin-safety-gate'
  config:
    protectedPaths:
      - .env
      - .env.local
      - .git/
      - id_rsa
      - node_modules/

- name: '@pi-cordis/plugin-git-guard'
  config:
    autoCheckpoint: true
    warnDirtyOnStart: true

- name: '@pi-cordis/plugin-rules-injector'

- name: '@pi-cordis/plugin-todo-tracker'
